import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import Icon from '.'

<Meta title="Icon" 
  component={Icon}
  />

# Icon组件

<br/> 

#### 基于fortawesome简单封装的Icon库

<Canvas key={1}>
  <Story name="default" args={{ 
    icon: 'male'
  }}>
    {args => <Icon {...args} />}
  </Story>
</Canvas>

##### 安装：
```sh
  npm install cola-comp --save
```
##### 基本用法：
```js
import Icon from './Icon'
<Icon icon='chevron-down'></Icon>
```

##### 各种类型Icon

<Canvas>
  <Icon icon='check'/>
  <Icon icon='check' size='lg'></Icon>
  <Icon icon='check' size='2x'></Icon>
  <Icon icon='check' theme='primary'/>
  <Icon icon='check' theme='secondary'/>
  <Icon icon='check' theme='success'/>
  <Icon icon='check' theme='warning'/>
  <Icon icon='check' theme='danger'/>
  <Icon icon='angle-down'/>
  <Icon icon='eye'/>
  <Icon icon='eye-slash'/>
  <Icon icon='search'/>
</Canvas>

## API
<Props of={Icon}/>



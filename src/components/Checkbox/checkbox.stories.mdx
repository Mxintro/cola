import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs/blocks'
import Checkbox from './'


<Meta title="Checkbox 多选框" 
  component={Checkbox}
  />

# Checkbox 多选框
<br/>

<br/>

<Checkbox onChange={(e) => console.log(`checked: ${e.target.checked}`)}>Checkbox</Checkbox>
<Checkbox disabled>disabled</Checkbox>

<br/>

#### 引入：
```tsx
  import { Checkbox } from 'cola-react-ui'
```
#### 基本用法：
```tsx
import { Checkbox } from 'cola-react-ui'

const MyCheckbox: React.FC = () => {
  return (
    <>
      <Checkbox onChange={(e) => console.log(`checked: ${e.target.checked}`)}>Checkbox</Checkbox>
      <Checkbox disabled>disabled</Checkbox>
      <Checkbox.Group
        options={[
          { label: 'Apple', value: 'Apple' },
          { label: 'Pear', value: 'Pear' },
          { label: 'Orange', value: 'Orange' },
        ]}
        defaultChecked={['Apple']}
        onChange={(res) => console.log(res)}
      />
    </>
  )
}
```

#### CheckboxGruop
##### 从数组生成Checkbox组，defaultValue默认选中的选项。

<Canvas>
  <Story name='CheckboxGruop' args={{
    options: [
      { label: 'Apple', value: 'Apple' },
      { label: 'Pear', value: 'Pear' },
      { label: 'Orange', value: 'Orange' },
    ],
    defaultChecked: ['Apple'],
    onChange: (res) => console.log(res)
  }}>
    {args => <Checkbox.Group {...args} />}
  </Story>
</Canvas>

## API
#### Checkbox
<Props of={Checkbox} />

#### Checkbox.Group
<Props of={Checkbox.Group} />




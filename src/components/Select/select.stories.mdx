import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs/blocks';
import Select, { Option } from './'


<Meta title="Select" 
  component={Select}
  />

# Select组件
#### 下拉选择器
<br/>

<Select style={{width: 300}} defaultValue='React'>
  <Select.Option value='react'>React</Select.Option>
  <Select.Option value='vue'>Vue</Select.Option>
  <Select.Option value='angular'>Angular</Select.Option>
  <Select.Option value='typescript'>TypeScript</Select.Option>
</Select>

#### 安装：

```sh
  npm install cola-comp --save
```
#### 基本用法：
```tsx
import Select from './Select'
const { Option } = Select

<Select
  style={{width: 300}}
  onSelect={(value)=> console.log(value)}
  >
  <Option value='vue'>Vue</Option>
  <Option value='react'>React</Option>
  <Option value='typeScript'>TypeScript</Option>
</Select>
```

#### 另一种使用方法，直接传入options数组，不需要配置Option组件

<Canvas>
  <Story name='select' args={{
    options:[{value:'java'},{value:'go'},{value:'c++'}],
    style: {width: 300}
  }}>
    {args => <Select {...args} />}
  </Story>
</Canvas>

## API
#### Select

<Props of={Select} />

#### Option

<Props of={Select.Option} />





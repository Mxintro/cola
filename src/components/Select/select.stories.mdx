import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs/blocks';
import Select, { Option } from './'


<Meta title="Select" 
  component={Select}
  />

# Select组件
#### 下拉选择器
<br/>

<Select style={{width: 300}} defaultValue='React'>
  <Select.Option value='react'>React</Select.Option>
  <Select.Option value='react'>Vue</Select.Option>
  <Select.Option value='react'>Angular</Select.Option>
  <Select.Option value='react'>TypeScript</Select.Option>
</Select>

#####
##### 安装：

```sh
  npm install cola-comp --save
```
##### 基本用法：
```tsx
import Select from './Select'
const { Option } = Select

<Select
  style={{width: 300}}
  onSelect={(value)=> console.log(value)}
  >
  <Option value='vue'>Vue</Option>
  <Option value='react'>React</Option>
  <Option value='typeScript'>TypeScript</Option>
</Select>
```

##### 各种类型Select

<Canvas>
  <Story name='select' args={{
  }}>
    {args => <Select style={{width: 300}} {...args}>
      <Option value='react'>React</Option>
    </Select>}
  </Story>
</Canvas>

## API
<Props of={Select} />



